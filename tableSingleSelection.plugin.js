(function($){

  var defaultSettings = {
    selectedClass: 'selected',
    targetSelector: 'a',
    onSelected: $.noop,
    isDisabled: $.noop
  }

  $.fn.tableSingleSelection = function(){

  };

}(jQuery));
